{
  "name": "arizta-server",
  "version": "1.0.0",
  "description": "Arizta - Real-time chat application server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx nodemon server.js",
    "atlas": "echo 'Starting server with Atlas connection...' && node server.js",
    "local": "echo 'Starting server with local MongoDB...' && MONGODB_URI=mongodb://localhost:27017/rtca-chat node server.js",
    "test-connection": "node -e \"require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(() => { console.log('✅ Connection successful'); process.exit(0); }).catch(err => { console.error('❌ Connection failed:', err.message); process.exit(1); });\"",
    "setup-atlas": "node setup-atlas.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.41.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.1",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.4.3",
    "multer": "^2.0.2",
    "multer-storage-cloudinary": "^4.0.0",
    "passport": "^0.6.0",
    "passport-google-oauth20": "^2.0.0",
    "passport-jwt": "^4.0.1",
    "sharp": "^0.34.3",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
